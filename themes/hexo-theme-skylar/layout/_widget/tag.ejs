<% if (site.tags.length){ %>
<div class="tagslist">
	<p class="asidetitle">大额的各种文章</p>
		<ul class="clearfix">
		<% site.tags.sort('posts.length','desc').limit(20).each(function(item){ %>
			<% if(item.posts.length){ %>
				<li><a href="<%- config.root %><%- item.path %>" title="<%= item.name %>"><%= item.name %><sup><%= item.posts.length %></sup></a></li>
			<% } %>
		<% }); %>
		</ul>
</div>
<% } %>
